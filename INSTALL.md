# INSTALL

## Requirements
- NVIDIA GPU with CUDA support
- CUDA Toolkit
- `nvcc`
- Python 3 for helper scripts (optional)

Optional Python packages for proof generation:
```bash
pip3 install pillow pandas
```

## Build
```bash
make
```

## Run
```bash
chmod +x run.sh
./run.sh
```
